import { FormControl, Input, InputLabel } from '@mui/material';

export function InputForm() {
  return (

    <FormControl>
      <InputLabel shrink>
        Text
      </InputLabel>
      <Input />
    </FormControl>

  );
}
